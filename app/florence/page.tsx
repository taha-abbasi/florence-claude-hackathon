import FlorenceRoot from "@/components/florence/FlorenceRoot";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Talk to Florence — your real, subsidized health-insurance price",
};

export default function FlorencePage() {
  return <FlorenceRoot />;
}
